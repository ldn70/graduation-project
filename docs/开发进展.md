# 开发进展（实时）

## 当前完成
- 后端：
  - Django + DRF + JWT 基础框架完成。
  - 已实现接口：
    - 用户：`/api/users/register`、`/api/users/login`、`/api/users/profile`、`/api/users/delete`、`/api/users/security-logs`
    - 搜索：`/api/jobs/search`
    - 推荐：`/api/recommend/jobs`
    - 技能：`/api/skills/demand`、`/api/skills/match`
    - 薪资：`/api/salary/predict`
    - 趋势：`/api/trends/jobs`
    - 简历：`/api/resume/generate`、`/download/<filename>`
  - 迁移与样例数据脚本可用：`manage.py migrate`、`manage.py seed_jobs`
  - 数据导入升级（`[DONE]`）：
    - `manage.py import_jobs_csv` 支持 CSV -> MySQL。
    - 新增可选 CSV -> MongoDB 同步（`--sync-mongo`，`job_texts` 集合）。
  - 推荐算法升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_recommend_model`。
    - 推荐链路升级为 Hybrid：TF-IDF + CF + LightFM（无 LightFM 时自动 SVD 降级）。
    - `/api/recommend/jobs` 优先使用训练模型，未训练时自动回退 baseline。
  - 薪资预测升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_salary_model`。
    - `/api/salary/predict` 优先使用 XGBoost 模型推理并返回 SHAP 解释。
    - 模型不可用时自动回退到 baseline 估算逻辑，接口不降级。
  - 趋势模型升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_trend_model`。
    - `/api/trends/jobs` 支持 `time_range=month|quarter|year` 聚合。
    - 月维度 `forecast=true` 优先使用 Hybrid（Prophet + LSTM 代理）推理，模型不可用时自动回退 baseline。
  - 自动化测试升级（`[DONE]`）：
    - 新增后端 API 回归测试：`users/jobs/resumes/analysis` 核心接口覆盖。
    - 已验证测试命令：`manage.py test`（当前 19 项全部通过）。
  - 全局异常处理器补齐（`[DONE]`）：
    - 新增 DRF 全局异常处理器：`backend/core/exceptions.py`，统一输出标准错误结构。
    - 常见异常状态统一补码：`401/403/404/500`（含 `COMMON_AUTH_REQUIRED` 等全局错误码）。
    - 新增 Django 级 `400/403/404/500` 兜底处理：`backend/core/http_error_handlers.py`。
  - 限流与频控统一错误码补齐（`[DONE]`）：
    - 启用 DRF 默认限流：匿名与登录用户速率可通过环境变量配置（`DRF_THROTTLE_ANON_RATE`、`DRF_THROTTLE_USER_RATE`）。
    - 全局节流异常统一输出 `429 + COMMON_TOO_MANY_REQUESTS`，并返回 `retry_after`。
    - 新增后端回归测试覆盖节流触发场景与统一响应结构。
  - 关键接口分级限流策略（`[DONE]`）：
    - 关键接口新增 `throttle_scope`：`auth_register/auth_login/jobs_search/analysis_*/resume_*`。
    - 默认策略实现“登录/注册更严格、查询类更宽松”的分级速率控制。
    - 新增接口级节流回归测试：登录接口与搜索接口均覆盖 `429 + COMMON_TOO_MANY_REQUESTS`。
  - 登录失败封禁防护（`[DONE]`）：
    - 登录接口新增失败计数：按账号与 IP 维度累计失败次数。
    - 达到阈值后触发临时封禁，返回 `429 + USER_LOGIN_TEMP_LOCKED`，并携带 `retry_after`。
    - 新增后端回归测试覆盖“连续失败触发封禁 + 封禁期间正确密码仍被拒绝”场景。
  - 认证安全审计日志（`[DONE]`）：
    - 新增 `AuthSecurityLog` 审计模型，记录登录失败、触发封禁、封禁拦截、登录成功、失败计数重置事件。
    - 登录链路已接入安全审计打点，异常不影响认证主流程。
    - 新增 `GET /api/users/security-logs`，支持按账号/IP/事件类型查询与分页。
    - 新增后端回归测试覆盖审计事件写入与查询接口筛选能力。
- 前端：
  - Vue3 页面已接入上述全部核心接口。
  - 已包含 ECharts 可视化（地域分布 + 趋势图）。
  - 前端模块拆分与按需加载优化（`[DONE]`）：
    - 图表能力改为异步加载（`echartsCore` 动态导入），避免首屏主包携带图表依赖。
    - Vite 构建增加 `manualChunks`，将 `echarts` 与 `zrender` 分包。
    - 构建产物由单一超大包优化为多 chunk（`index` ~113KB，`vendor-echarts` ~363KB，`vendor-zrender` ~179KB），已消除 chunk 超限告警。
  - 前端测试与展示升级（`[DONE]`）：
    - 新增 Vitest 关键流程测试（登录/搜索/趋势交互）。
    - 趋势模块新增 `time_range` 选择、`model_info` 展示与预测区间可视化（上界/下界）。
  - 前端异常态/空数据态测试补充（`[DONE]`）：
    - 新增搜索 API 异常态 UI 测试（错误提示文案校验）。
    - 新增搜索空数据态 UI 测试（`total=0` 与列表空态校验）。
    - 当前前端测试总计 7 项，均通过。
  - 页面级懒加载拆分（`[DONE]`）：
    - 引入 `vue-router`，将单页拆分为 `Auth/Search/Recommend/Salary/Trends` 5 个路由视图。
    - 路由组件全部改为动态导入，构建产物新增页面级 chunk（如 `SearchView`、`TrendsView` 等）。
    - 保持图表按需加载策略，趋势页才会触发 ECharts 相关 chunk 下载。
  - 关键面板异常交互增强（`[DONE]`）：
    - Search/Recommend/Salary/Trends 面板新增可操作提示卡片（`warning/error/info`）。
    - 未登录/无权限时展示“去登录”入口；超时与错误时展示“重试”动作。
    - 技能匹配前置条件不足（未先搜索岗位）时，提供明确引导提示。
  - 关键请求显式加载态（`[DONE]`）：
    - Auth/Search/Recommend/Salary/Trends 核心动作按钮新增 loading 与 disabled 状态，防止重复点击。
    - Search/Recommend/Salary/Trends 面板新增 skeleton 骨架屏，弱网下保留可感知反馈。
  - 错误提示粒度细化（`[DONE]`）：
    - 前端统一错误分类逻辑新增 `400/429/5xx/超时/鉴权` 分级文案。
    - 错误提示与 Toast 文案统一来源于分类结果，避免“仅提示失败”的模糊反馈。
  - 错误提示场景化文案补齐（`[DONE]`）：
    - 按业务接口补齐文案模板，覆盖“参数缺失/格式错误/权限不足/频率限制”等用户可行动提示。
    - 对推荐、薪资、趋势等核心接口统一输出“分类前缀 + 业务提示”样式，弱化纯技术报错。
    - 新增前端测试覆盖 400 参数缺失与格式错误场景，确保提示文案可回归。
  - 错误码驱动文案映射（`[DONE]`）：
    - 后端关键错误响应新增 `code` 字段（用户/搜索/推荐/技能匹配/简历等接口）。
    - 前端错误提示逻辑升级为“优先按后端 `code` 映射，关键词仅作为兜底”。
    - 新增前端测试覆盖按 `code` 映射的搜索参数错误与登录凭证错误场景。
  - 统一错误码规范文档补齐（`[DONE]`）：
    - 新增 `docs/错误码规范.md`，明确错误码命名规则、响应约定与维护流程。
    - 形成接口错误码覆盖清单（已覆盖/未覆盖）并与当前实现对齐。
    - 补齐前端错误码映射表与兜底规则说明，确保跨端一致。
  - salary/trends/download 错误码补齐（`[DONE]`）：
    - 薪资接口补齐 `SALARY_SKILLS_REQUIRED`、`SALARY_SKILLS_INVALID_FORMAT`。
    - 趋势接口补齐 `TREND_TIME_RANGE_INVALID`、`TREND_FORECAST_INVALID`。
    - 下载接口补齐 `RESUME_FILE_NOT_FOUND`，文件不存在返回统一错误结构。
    - 新增后端回归测试覆盖以上错误码，并补齐前端按 `code` 映射提示测试。
  - 用户删除与下载异常错误码补齐（`[DONE]`）：
    - 用户删除接口补齐 `USER_DELETE_AUTH_REQUIRED`，未登录时返回统一错误结构。
    - 下载接口补齐 `RESUME_FILE_NAME_INVALID`、`RESUME_FILE_READ_FAILED`。
    - 新增后端回归测试覆盖“未登录注销”与“下载读取失败”场景。
    - 前端错误码映射补齐新增错误码，保持规范对齐。
  - 空结果成功态引导补齐（`[DONE]`）：
    - 推荐面板新增“请求成功但无推荐结果”的引导文案。
    - 薪资面板新增“请求成功但无可展示薪资区间”的引导文案。
    - 趋势面板新增“请求成功但无历史/预测数据”的引导文案。
    - 新增前端测试覆盖推荐/薪资/趋势空结果成功态，确保引导文案可回归。
  - 空结果成功态可执行动作补齐（`[DONE]`）：
    - 推荐/技能需求/薪资/趋势面板的空结果成功态新增“去搜索页”动作按钮。
    - 新增统一“重置筛选”动作，支持一键清空搜索筛选条件并提示重新查询。
    - 新增前端测试校验空结果态按钮渲染与“重置筛选”动作生效。
  - 路由端到端冒烟测试（`[DONE]`）：
    - 新增 `App + Router` 冒烟测试，覆盖跨路由切换（`/search -> /trends -> /recommend`）的数据加载触发。
    - 增加路由往返状态保持测试，验证搜索条件与结果在页面切换后仍可保持。
    - 测试体系现为 2 个文件共 20 项（均通过）。
  - 全局错误码提示映射补齐（`[DONE]`）：
    - 前端新增 `COMMON_AUTH_REQUIRED/COMMON_AUTH_INVALID/COMMON_PERMISSION_DENIED/COMMON_RESOURCE_NOT_FOUND/COMMON_INTERNAL_SERVER_ERROR` 显式映射。
    - 避免全局认证错误码被“`*_REQUIRED` 兜底规则”误判为“参数缺失”。
    - 新增前端测试覆盖 `COMMON_AUTH_REQUIRED` 场景的提示与跳转行为。
  - 频控错误码映射补齐（`[DONE]`）：
    - 前端新增 `COMMON_TOO_MANY_REQUESTS` 显式映射，统一输出“频率限制”提示。
    - 新增前端测试覆盖 `429 + COMMON_TOO_MANY_REQUESTS` 的提示回归。
  - 登录封禁提示映射补齐（`[DONE]`）：
    - 前端新增 `USER_LOGIN_TEMP_LOCKED` 显式映射，统一输出“登录受限”提示。
    - 新增前端测试覆盖登录接口 `429 + USER_LOGIN_TEMP_LOCKED` 场景。
- 工程化：
  - `docker-compose.yml`、前后端 `Dockerfile` 已创建。
  - 根目录 `README.md` 可指导启动。

## 已验证
- 后端 `manage.py check` 通过。
- 后端 `manage.py test` 通过（31 项）。
- 前端 `npm run build` 通过。
- 前端 `npm test` 通过（20 项）。
- 关键接口本地冒烟通过（状态码 200/201）。

## 下一步（建议按优先级）
1. 提供审计日志导出能力（CSV）与按时间区间查询，便于答辩演示和安全复盘。
