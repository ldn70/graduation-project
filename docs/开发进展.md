# 开发进展（实时）

## 当前完成
- 后端：
  - Django + DRF + JWT 基础框架完成。
  - 已实现接口：
    - 用户：`/api/users/register`、`/api/users/login`、`/api/users/profile`、`/api/users/delete`
    - 搜索：`/api/jobs/search`
    - 推荐：`/api/recommend/jobs`
    - 技能：`/api/skills/demand`、`/api/skills/match`
    - 薪资：`/api/salary/predict`
    - 趋势：`/api/trends/jobs`
    - 简历：`/api/resume/generate`、`/download/<filename>`
  - 迁移与样例数据脚本可用：`manage.py migrate`、`manage.py seed_jobs`
  - 数据导入升级（`[DONE]`）：
    - `manage.py import_jobs_csv` 支持 CSV -> MySQL。
    - 新增可选 CSV -> MongoDB 同步（`--sync-mongo`，`job_texts` 集合）。
  - 推荐算法升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_recommend_model`。
    - 推荐链路升级为 Hybrid：TF-IDF + CF + LightFM（无 LightFM 时自动 SVD 降级）。
    - `/api/recommend/jobs` 优先使用训练模型，未训练时自动回退 baseline。
  - 薪资预测升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_salary_model`。
    - `/api/salary/predict` 优先使用 XGBoost 模型推理并返回 SHAP 解释。
    - 模型不可用时自动回退到 baseline 估算逻辑，接口不降级。
  - 趋势模型升级（`[DONE]`）：
    - 新增离线训练命令：`manage.py train_trend_model`。
    - `/api/trends/jobs` 支持 `time_range=month|quarter|year` 聚合。
    - 月维度 `forecast=true` 优先使用 Hybrid（Prophet + LSTM 代理）推理，模型不可用时自动回退 baseline。
  - 自动化测试升级（`[DONE]`）：
    - 新增后端 API 回归测试：`users/jobs/resumes/analysis` 核心接口覆盖。
    - 已验证测试命令：`manage.py test`（当前 19 项全部通过）。
- 前端：
  - Vue3 页面已接入上述全部核心接口。
  - 已包含 ECharts 可视化（地域分布 + 趋势图）。
  - 前端模块拆分与按需加载优化（`[DONE]`）：
    - 图表能力改为异步加载（`echartsCore` 动态导入），避免首屏主包携带图表依赖。
    - Vite 构建增加 `manualChunks`，将 `echarts` 与 `zrender` 分包。
    - 构建产物由单一超大包优化为多 chunk（`index` ~113KB，`vendor-echarts` ~363KB，`vendor-zrender` ~179KB），已消除 chunk 超限告警。
  - 前端测试与展示升级（`[DONE]`）：
    - 新增 Vitest 关键流程测试（登录/搜索/趋势交互）。
    - 趋势模块新增 `time_range` 选择、`model_info` 展示与预测区间可视化（上界/下界）。
  - 前端异常态/空数据态测试补充（`[DONE]`）：
    - 新增搜索 API 异常态 UI 测试（错误提示文案校验）。
    - 新增搜索空数据态 UI 测试（`total=0` 与列表空态校验）。
    - 当前前端测试总计 7 项，均通过。
  - 页面级懒加载拆分（`[DONE]`）：
    - 引入 `vue-router`，将单页拆分为 `Auth/Search/Recommend/Salary/Trends` 5 个路由视图。
    - 路由组件全部改为动态导入，构建产物新增页面级 chunk（如 `SearchView`、`TrendsView` 等）。
    - 保持图表按需加载策略，趋势页才会触发 ECharts 相关 chunk 下载。
  - 关键面板异常交互增强（`[DONE]`）：
    - Search/Recommend/Salary/Trends 面板新增可操作提示卡片（`warning/error/info`）。
    - 未登录/无权限时展示“去登录”入口；超时与错误时展示“重试”动作。
    - 技能匹配前置条件不足（未先搜索岗位）时，提供明确引导提示。
  - 路由端到端冒烟测试（`[DONE]`）：
    - 新增 `App + Router` 冒烟测试，覆盖跨路由切换（`/search -> /trends -> /recommend`）的数据加载触发。
    - 增加路由往返状态保持测试，验证搜索条件与结果在页面切换后仍可保持。
    - 测试体系现为 2 个文件共 9 项（均通过）。
- 工程化：
  - `docker-compose.yml`、前后端 `Dockerfile` 已创建。
  - 根目录 `README.md` 可指导启动。

## 已验证
- 后端 `manage.py check` 通过。
- 前端 `npm run build` 通过。
- 前端 `npm test` 通过（9 项）。
- 关键接口本地冒烟通过（状态码 200/201）。

## 下一步（建议按优先级）
1. 为关键请求增加显式加载态（按钮 loading 与面板 skeleton），进一步改善弱网体验。
2. 细化错误提示粒度（按接口区分“参数错误/服务不可用/频率限制”）。
